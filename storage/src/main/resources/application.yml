server:
  port: 8080
  servlet:
    context-path: /storage

# 存储服务配置
storage:
  default: ceph-s3
  providers:
    - type: ceph-s3
      enabled: true
    - type: hdfs
      enabled: false
    - type: minio
      enabled: false

# AWS S3配置（用于Ceph连接）
aws:
  s3:
    accessKey: ZUNV3E08FVAQPSWC1Q41
    secretKey: dacVadaDCVTZ3RmGnv2PSFVKSyOgM9lSAaSUGxuw
    region: us-east-1
    endpoint: http://10.170.6.40:31033
    bucket: default

# Spring配置
spring:
  application:
    name: im-storage-service
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

# 日志配置
logging:
  level:
    com.im.storage: DEBUG
    software.amazon.awssdk: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
