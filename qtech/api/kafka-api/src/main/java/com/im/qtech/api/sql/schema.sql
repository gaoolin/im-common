CREATE TABLE MESSAGE_BACKUP
(
    ID          NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    TOPIC       VARCHAR2(255) NOT NULL,
    PARTITION   VARCHAR2(255) NOT NULL,
    OFFSET      VARCHAR2(255) NOT NULL,
    KEY         VARCHAR2(255),
    PAYLOAD     CLOB,
    RECEIVED_AT TIMESTAMP NOT NULL
);
